# Hermes Entry

Hermes must use [[MATCHER]] as the canonical project protocol.

Required sequence:

1. Read [[MATCHER]].
2. If using skills, prefer `.hermes/skills/community-matcher/SKILL.md`.
3. Use the shared templates exposed under `.hermes/skills/community-matcher/assets/`.
4. Use the shared schema exposed under `.hermes/skills/community-matcher/references/`.
5. After edits, run `bash scripts/lint.sh`.
